Preferences preferences; // Initialize preferences used to store variables that survive poweroff

bool Alarm1;        // Variable that signifies whether or not the alarm is set
bool Alarm2;        // Variable that signifies whether or not the alarm is set
bool Alarm3;        // Variable that signifies whether or not the alarm is set
bool Alarm4;        // Variable that signifies whether or not the alarm is set
bool Alarm5;        // Variable that signifies whether or not the alarm is set
int hourAlarm1;          // Store the alarm hour
int minAlarm1;           // Store the alarm minute
int hourAlarm2;          // Store the alarm hour
int minAlarm2;           // Store the alarm minute
int hourAlarm3;          // Store the alarm hour
int minAlarm3;           // Store the alarm minute
int hourAlarm4;          // Store the alarm hour
int minAlarm4;           // Store the alarm minute
int hourAlarm5;          // Store the alarm hour
int minAlarm5;           // Store the alarm minute
